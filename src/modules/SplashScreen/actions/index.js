// @flow
import { createActions } from 'redux-actions';

// eslint-disable-next-line import/prefer-default-export
export const {
  loadSplashScreen,
} = createActions({
  LOAD_SPLASH_SCREEN: () => ({}),
});
